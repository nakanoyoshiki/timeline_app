<%= link_to favorite_path(user_favorite(post), post_id: post.id), remote: true, method: :delete ,class: 'btn btn-primary pull-right btn-lg outline data-updater btn-favorite-delete' do %>
    <%= image_tag 'fav_btn.gif' %>
<% end %>