<%= link_to favorites_path(post_id: post.id), remote: true, method: :post ,class: 'btn btn-primary pull-right btn-lg outline data-updater btn-favorite-add' do %>
    <%= image_tag 'no_fav_btn.gif' %>
<% end %>